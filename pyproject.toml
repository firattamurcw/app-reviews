[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "app-reviews"
version = "0.6.0"
description = "The go-to Python package for App Store and Google Play review extraction"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Firat Tamur" },
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Typing :: Typed",
]
dependencies = [
    # Ranges, not pins. A library's `==` becomes every consumer's `==`: when a
    # CVE lands in either of these, an application depending on app-reviews
    # cannot take the patch until app-reviews cuts a release, and pip either
    # hard-conflicts or resolves the whole environment onto the vulnerable
    # version. Exact versions still live in uv.lock, which is what CI installs,
    # so reproducibility is unaffected.
    #
    # Upper bounds are real: httpx is pre-1.0, so a minor bump may break, and
    # the redirect behaviour this package relies on (Authorization dropped on
    # origin change) is httpx's, not ours. test_http.py pins it, so an
    # in-range upgrade that changed it fails the suite rather than shipping.
    "cryptography>=49,<51",
    "httpx>=0.28,<0.29",
]

[project.urls]
Homepage = "https://github.com/firattamurcw/app-reviews"
Repository = "https://github.com/firattamurcw/app-reviews"
Issues = "https://github.com/firattamurcw/app-reviews/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/app_reviews"]

[dependency-groups]
dev = [
    "pytest==9.1.1",
    "pytest-cov==7.1.0",
    "pytest-asyncio==1.4.0",
    "mypy==2.3.0",
    "ruff==0.15.22",
    "pre-commit==4.6.1",
    "mkdocs-material>=9.6",
]

[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]

[tool.ruff.lint.isort]
known-first-party = ["app_reviews"]

[tool.mypy]
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = ["live: live E2E tests that hit real store endpoints (skipped by default)"]
addopts = "-m 'not live'"

[tool.coverage.run]
source = ["app_reviews"]
branch = true

[tool.coverage.report]
fail_under = 85
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
]
